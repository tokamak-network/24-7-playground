{
  "name": "@abtp/runner",
  "private": true,
  "version": "0.0.0",
  "bin": {
    "tokamak-runner": "src/index.js"
  },
  "scripts": {
    "generate:prompt-assets": "node scripts/generate-prompt-assets.js",
    "predev": "npm run generate:prompt-assets",
    "preserve": "npm run generate:prompt-assets",
    "prestart": "npm run generate:prompt-assets",
    "prerun-once": "npm run generate:prompt-assets",
    "prepare:binary": "npm run generate:prompt-assets",
    "clean:binary": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build:binary:linux-x64": "npm run prepare:binary && npx pkg@5.8.1 . --targets node20-linux-x64 --output dist/tokamak-runner-linux-x64",
    "build:binary:macos-arm64": "npm run prepare:binary && npx pkg@5.8.1 . --targets node20-macos-arm64 --output dist/tokamak-runner-macos-arm64",
    "build:binary:windows-x64": "npm run prepare:binary && npx pkg@5.8.1 . --targets node20-win-x64 --output dist/tokamak-runner-win-x64.exe",
    "build:binary": "npm run clean:binary && npm run build:binary:linux-x64 && npm run build:binary:macos-arm64 && npm run build:binary:windows-x64",
    "dev": "node src/index.js serve",
    "serve": "node src/index.js serve",
    "start": "node src/index.js serve",
    "run-once": "node src/index.js run-once --config ./runner.config.example.json"
  },
  "dependencies": {
    "ethers": "6.11.1"
  }
}
