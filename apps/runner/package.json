{
  "name": "@agentic-ethereum/runner",
  "private": false,
  "version": "0.2.1",
  "description": "Local multi-agent runner for Agentic Ethereum: 24-7 Playground.",
  "license": "MIT",
  "homepage": "https://agentic-ethereum.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tokamak-network/24-7-playground.git",
    "directory": "apps/runner"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "src",
    "scripts",
    "prompts",
    "runner.config.example.json",
    "README.md"
  ],
  "keywords": [
    "ethereum",
    "ai-agents",
    "runner",
    "sepolia",
    "smart-contract",
    "llm"
  ],
  "bin": {
    "tokamak-runner": "src/index.js"
  },
  "scripts": {
    "generate:prompt-assets": "node scripts/generate-prompt-assets.js",
    "clean:build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build:local": "npm run clean:build && npm run generate:prompt-assets && node scripts/build-local-binary.js",
    "bootstrap:build": "node scripts/bootstrap-build.js",
    "bootstrap:build:all": "node scripts/bootstrap-build.js --all",
    "build:linux-x64": "npm run generate:prompt-assets && npx pkg@5.8.1 . --targets node18-linux-x64 --output dist/tokamak-runner-linux-x64",
    "build:macos-arm64": "npm run generate:prompt-assets && npx pkg@5.8.1 . --targets node18-macos-arm64 --output dist/tokamak-runner-macos-arm64",
    "build:windows-x64": "npm run generate:prompt-assets && npx pkg@5.8.1 . --targets node18-win-x64 --output dist/tokamak-runner-win-x64.exe",
    "build": "npm run clean:build && npm run build:linux-x64 && npm run build:macos-arm64 && npm run build:windows-x64",
    "dev": "npm run generate:prompt-assets && node src/index.js serve",
    "start": "node scripts/start-binary.js",
    "inspect:managed": "node scripts/inspect-managed-agents.js",
    "run-once": "node src/index.js run-once --config ./runner.config.example.json"
  },
  "dependencies": {
    "ethers": "6.11.1"
  }
}
